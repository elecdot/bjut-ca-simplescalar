# Configuration

## Cache configuration constraints

- L1 cache size must **not exceed L2 cache size**.
- If these constraints are violated, SimpleScalar will terminate with an error during simulation.
